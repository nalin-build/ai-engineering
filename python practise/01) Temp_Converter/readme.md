We built a menu-based temperature converter (1 = F to C, 2 = C to F, 3 = quit) that runs in a loop until the user quits. It asks the user which conversion they want, then asks for the value to convert, or lets them quit. Used separate functions to handle each conversion instead of putting the math directly in main().

Topics used: functions, loops, conditional statements