Collects numbers from user input into a list until they type "done," then prints min/max/average.

Mistakes: used numbers(i) instead of numbers[i] at first - parentheses call a function, brackets index a list.
Also named my result variables max/min, which shadowed Python's built-in max()/min() functions and printed "<built-in function max>" instead of my number - had to rename to maximum/minimum.

Topics: functions, loops, lists, conditionals, exceptions



hint: be careful as to what happens if the user types in done at the very start.
hint: what if the user types any other string other than done and how to know if it is a actually a number.

